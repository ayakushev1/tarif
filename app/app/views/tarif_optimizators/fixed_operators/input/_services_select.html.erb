<%
form_item.init do |f|
  f.set_fields do |filtr_name|
	input_html = {:filtr_name => form_item.filtr_name, :class => "updatable form-control ", :action_name => tarif_optimizators_fixed_operators_index_path}
	label_html = {:class => "control-label "}
    s = session_filtr_params(f)
	
  [
  	[
     label(filtr_name, "tel", "Теле2", label_html),
 	 check_box(filtr_name, "tel", (s["tel"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false ),

     label(filtr_name, "bln", "Билайн", label_html),
 	 check_box(filtr_name, "bln", (s["bln"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false ),

     label(filtr_name, "mgf", "Мегафон", label_html),
 	 check_box(filtr_name, "mgf", (s["mgf"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false ),

     label(filtr_name, "mts", "МТС", label_html),
 	 check_box(filtr_name, "mts", (s["mts"] == 'true' ? {:checked => 'checked'} : {} ).merge(input_html), true, false ),
  	],

  ]
	    
  end
end
%>
