<%= active if false %>
<%= session[:current_tabs_page]  if false %>

<%
name ||= 'tabs' 
active ||= -1 
i = 0

@content_from_tabs = []
content_from :tabs do |caption, body, type| 
  @content_from_tabs << [caption, body, type]
end 

%>

<nav class="navbar navbar-default ">
	<div class="container-fluid">

	    <div class="navbar-header">
	      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#<%= name %>" aria-expanded="false">
	    	<span class="sr-only">dddd</span>
			<% @content_from_tabs.each do |caption, body| %>
		    	<span class="icon-bar"></span>
		    	<% i += 1 %>
		    <% end %>
	      </button>
	      <a class="navbar-brand visible-xs-block" href="#"><%= brand_name %></a>
	    </div>
	    
		<% i = 0 %>
		
	   <div class="collapse navbar-collapse" id="<%= name %>">
			<ul class="nav navbar-nav">
				<% @content_from_tabs.each do |caption, body, type| %>
					<% case type %>
					<% when 'dropdown-menu' %>
				    	<li class="dropdown <%= (i==active) ? 'active' : '' %>">
				    		<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">			    			
				    				<%= caption %>				    			
				    		</a>
			    			<%= body %>
				    	</li>
					<% when 'link' %>
						<li><%= body %></li>						
					<% else %>
						<%= body %>
					<% end %>
					
			    	<% i += 1 %>
			    <% end %>
			</ul>
		</div>		
	</div>
</nav>

