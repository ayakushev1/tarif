<%
table.init do |t|
#  t.caption = 'Список рекомендаций'
#  t.base_name = 'content_articles'
#  t.pagination_per_page = 10
#  t.id_name = 'id'#'service_category_tarif_class_id'
  
#  t.heads = ['id', 'author_id', 'title', 'content', 'type_id', 'status_id', 'key', '']
  t.heads = [ '', 'Название рекомендации', 'Пользовательская модель',]
  
  t.set_fields do |f| 
  	[
#  	 f['id'],
#  	 f['author_id'],
     link_to('', content_articles_show_path, 
     	{:remote => true, :class => "fa fa-random fa-lg center", :'aria-hidden' => "true", :'aria-label' => "Подробное описание"}),
  	 f['title'],
  	 (f['content']['usage_model'] if f['content']),
#  	 (f['content']['summary'] if f['content']),
#  	 (f['content']['body'] if f['content']),
#     f['type_id'],
#     f['status_id'],
#     f['key'],
  	] 
  end
end
%>
