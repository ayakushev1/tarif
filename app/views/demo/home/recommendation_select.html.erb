<% content_for :title, "Выбор рекомендации по тарифам и опциям" %>

<% recommendation = Content::Article.demo_results.where(:id => controller.demo_result_id).first %>

<p><strong>Выбранный вариант: </strong><i><%= recommendation.blank? ? "Рекомендация не выбрана" : recommendation['title'] %></i></p>

<!--
<p><strong>Краткое описание: </strong><%=  recommendation['content']['summary'] if recommendation and recommendation['content'] %></p>
-->

<%= render(:partial => 'demo/home/recommendation_select/params', :layout => 'application/layouts/demo/filtr_form_2_columns', 
      :locals => {:form_item => controller.recommendation_select_params}) %>

<div class="row">
	<div class="col-xs-2">
		<%= link_to('Перейти к рекомендации', demo_home_demo_results_path, {:class => "btn btn-success", :type =>"button"}) %> 
	</div>
</div>
