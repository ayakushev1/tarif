<%
table.init do |t|
  link_html = {:remote => true}
  t.heads = if user_type == :admin
  	['Название', 'Описание сравнения', 'Статус публикации', 'Порядок публикации', 'optimization list key', '', '', '', ]
  else
  	['Название', 'Описание сравнения операторов и тарифов', '']
  end 
  
  t.set_fields do |result| 
  [
    result.name,
    result.description,
    link_to( 'Просмотр', comparison_result_path(result), link_html), 
  ] + (user_type != :admin ? [] : [
    (result.publication_status.name if result.publication_status), 
    result.publication_order,
    result.optimization_list_key,
    link_to( 'Редактировать', edit_comparison_result_path(result), link_html), 
    link_to( 'Удалить', result, method: :delete, data: { confirm: 'Are you sure?'}),
#  	result.id, 
  	])
  end
end
%>
