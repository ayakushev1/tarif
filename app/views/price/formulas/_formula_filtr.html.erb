<%
form_item.init do |fi|
  s = session_filtr_params(fi)	
  fi.caption = "Price formula filtr fields"	
  fi.set_fields do |filtr_name|
  	action = {:action_name => price_formulas_path} 
    [
#      ["", collection_select(filtr_name, "price_list_id", PriceList.where(:id => Price::Formula.query_from_filtr(s, "price_list_id").pluck(:price_list_id).uniq ), :id, :name, 
#	  	{:include_blank => "price list", :selected => s["price_list_id"] }, action )
#	  ],

      ["", collection_select(filtr_name, "standard_formula_id", Price::StandardFormula.where(:id => Price::Formula.query_from_filtr(s, "standard_formula_id").pluck(:standard_formula_id).uniq ), :id, :name, 
	  	{:include_blank => "standard formula", :selected => s["standard_formula_id"] }, action )
	  ],
    ] 
  end
end
%>
