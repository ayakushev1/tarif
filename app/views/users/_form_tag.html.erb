<%
form_item.init do |f|
  f.action_on_submit = (controller.action_name.to_s == 'new' ? users_path : user_path)	
  f.set_fields do |form_name|
	  [ 
	   [label(form_name, :name), text_field(form_name, :name)],
	   [label(form_name, :password), password_field(form_name,  :password, :value => "")], 
	   [label(form_name, :password_confirmation), password_field(form_name, :password_confirmation)],
	   [label(form_name, " "), submit_tag()]
	  ]
  end
end
%>
