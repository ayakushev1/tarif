<%

content_in :fields do |f|
  [ 
   [f.label( :name), f.text_field( :name)],
   [f.label( :password), f.password_field( :password, :value => "")], 
   [f.label( :password_confirmation), f.password_field( :password_confirmation)],
   [f.label( " "), f.submit]
  ]
end
%>
