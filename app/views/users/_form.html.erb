<%

form_item.init do |fi|
  action_path = fi.model.id ? edit_user_path(fi.model) : new_user_path	
  action = {:action_name => action_path }
  fi.set_fields do |f|
 	  [
	   [f.label( :name, "Электронный адрес"), 
	   	(fi.model.id ? f.label( :name, fi.model.name) : f.text_field( :name, action))],
	   [f.label( :password, "Пароль"), f.password_field( :password, action)], 
	   [f.label( :password_confirmation, "Подтверждение пароля"), f.password_field( :password_confirmation, action)],
	   [f.label( " "), f.submit()],
	  ]
  end
end
%>
