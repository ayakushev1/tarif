<div class="navbar-collapse">
	<div class="container-fluid">
	  <ul class="nav navbar-nav navbar-right">
	  	<% if current_or_guest_user %>
		    <li><%= link_to(new_customer_demand_path, {:class => "hidden-xs"}) do %>
		    	<span class="icon_menu_img"><%= image_tag("envelope_img.png") %></span>Письмо администратору
		        <% end %>
		    </li>
		<% end %> 
	
	    <li class="dropdown">
	    	<a href="#" class="dropdown-toggle" data-toggle="dropdown" >
	    		<span class="icon_menu_img">
	    			<%= image_tag("user_img.png") %>
	    		</span>Пользователь: 
	    		<span><%= current_user ? current_user.name : 'Гость' %></span>
	    		<span class="caret"></span>
	    	</a>
	        <ul class="dropdown-menu user-dropdown-menu" role="menu">
				<% if !current_user %>
					<li><%= link_to 'Войти', login_path %></li>
				<% else %>
					<li><%= link_to 'Профиль', user_path(controller.current_user) %></li>
					<li><%= link_to 'Выйти', logout_path %></li>
				<% end %>
				    <li><%= link_to('Письмо администратору', new_customer_demand_path, {:class => "visible-xs-block"}) %></li>
	        </ul>
	    </li>	    
	  </ul>
	</div>
</div>

