<%
table.init do |t|
#  t.caption = 'Listing users'
  link_html = {:remote => true}
  t.heads = if user_type == :admin
  	['Название', 'Описание звонков', 'Календарный период', 'Тип подбора', 'Номер подбора','', '', '', '', '', '', '', '']
  else
  	['Название', 'Описание звонков', 'Календарный период', 'Тип подбора', 'Номер подбора','', '', '', '']
  end 
  
  t.set_fields do |run| 
  [
    run.name,
#    run.description,
    (run.call_run.name if run.call_run), 
    run.accounting_period,
    run.optimization_type,
    run.run,
    link_to( t('result.runs.index.show_link'), result_run_path(run), link_html), 
    link_to( t('result.runs.index.edit_link'), edit_result_run_path(run), link_html), 
    link_to( t('result.runs.index.show_result_link'), result_service_sets_result_path(run), link_html), 
    link_to( t('result.runs.index.compare_result_link'), result_compare_path(run.id), link_html), 
  ] + (user_type != :admin ? [] : [
    link_to( 'Удалить', run, method: :delete, data: { confirm: 'Are you sure?'}),
#  	run.id, 
  	run.optimization_type_id,
  	run.comparison_group_id, 
  	(run.user.name if run.user),
  	])
  end
end
%>
