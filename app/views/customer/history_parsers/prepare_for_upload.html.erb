<% content_for :title, t('.title') %>
<% content_for :hide_title, 'true' %>

<header id="first">
    <div class="header-content header-content-smaller">
        <div class="inner">
            <h1 class="cursive"><%= t('.title') %></h1>
            <h5>Можно загрузить детализации звонков для <strong>МТС</strong> в формате HTML, XLS или XLSX,
			<strong>Мегафон</strong> в формате HTML, <strong>Билайн</strong> в формате XLS или XLSX, 
			<strong>Теле2</strong> в формате PDF.</h5>
			<% session[:current_accordion_page]['home_steps_accord'] = 0 %>
			<h5><%= link_to "Инструкция по получению детализации на сайтах операторов", home_detailed_description_path %></h5>
            <hr>
           	<%= render :partial => 'upload_form' %>
        </div>
    </div>
</header>


<p>
	<% render(:partial => 'customer/history_parsers/user_params', :layout => 'application/layouts/filtr_form_n_columns', 
		:locals => {:form_item => user_params_filtr})  if false %>
</p>


<div class="panel panel-default" >
	<div class="panel-heading"><h4>Сохранение результатов загрузки детализации</h4></div>
	<div class="panel-body">
		<%= render :partial => 'call_run_choice' %>
	</div>
	<div class="panel-footer">
		<%= link_to(t('customer.history_parsers.prepare_for_upload.call_runs_link'), customer_call_runs_path, {:class => "btn btn-primary", :type =>"button"}) %>		
		<%= render :partial => 'information' %>
	</div>
</div>

<p>
<%= render( :partial => 'call_history_tabs', :layout => 'application/layouts/tabs', 
      :locals => {:name => 'call_history_tabs', :active => session[:current_tabs_page]['call_history_tabs'].to_i}) if user_type == :admin %>
</p>

