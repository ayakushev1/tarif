<%
table.init do |t|
#  t.caption = 'Listing users'
  t.heads = ['Название', 'Источник данные', 'Описание', 'Оператор', ] + 
  			(user_type == :admin ? ["Пользователь", ''] : []) + ['', '', '',]
  t.set_fields do |call_run| 
  [
    call_run.name,
    call_run.source_name,
    call_run.description,
    (call_run.operator.name if call_run.operator),      
  ] + (user_type != :admin ? [] : [
  	(call_run.user.name if call_run.user and user_type == :admin),
    link_to( 'Удалить', call_run, method: :delete, data: { confirm: 'Are you sure?'}),
#  	call_run.id, 
  	]) + [
    link_to( t('customer.call_runs.index.call_runs.show_link'), customer_call_run_path(call_run), ), 
    link_to( t('customer.call_runs.index.call_runs.edit_link'), edit_customer_call_run_path(call_run)), 
    link_to( t('customer.call_runs.index.call_runs.call_stat_link'), customer_call_stat_path(call_run)), 
  	]
  end
end
%>
