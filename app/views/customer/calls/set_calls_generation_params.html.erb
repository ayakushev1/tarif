<% content_for :title, t('.title') %>
<% content_for :hide_title, 'true' %>

<header id="first">
    <div class="header-content header-content-smaller">
        <div class="inner">
            <h1 class="cursive"><%= t('.title') %></h1>
            <h5>С помощью <strong>моделирования истории звонков</strong> вы можете подобрать оптимальный тариф и тарифные опции
				на основании вашей желаемой модели пользования мобильными услугами</h5>
			<%= link_to(t('customer.calls.set_calls_generation_params.generate_calls_link'), customer_calls_generate_calls_path, 
				{:class => "btn btn-primary btn-xl", :type =>"button"}) %>
            <hr>
			<%= link_to("Перейти к загрузке детализации", customer_history_parsers_prepare_for_upload_path, 
				{:class => "btn btn-success", :type =>"button"}) %>
            <hr>
        </div>
    </div>
</header>


<div class="panel panel-default" >
	<div class="panel-heading"><h4> Сохранение результатов моделирования звонков</h4></div>
	<div class="panel-body">
		<%= render :partial => 'call_run_choice' %>
	</div>
	<div class="panel-footer">
		<%= link_to(t('customer.calls.set_calls_generation_params.call_runs_link'), customer_call_runs_path, 
			{:class => "btn btn-primary", :type =>"button"}) %>
		<%= render :partial => 'information' %>
	</div>
</div>

<h4>Параметры моделирования звонков</h4>


<%= render :partial => 'calls_gener_params_tabs', :layout => 'application/layouts/tabs', 
	:locals => {:name => 'calls_gener_params_tabs', :active => session[:current_tabs_page]['calls_gener_params_tabs'].to_i} %>


